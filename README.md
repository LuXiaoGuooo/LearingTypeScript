typescript 是javascript的超集
学习环境配置
首先下载ts环境，以及查看对应版本
1.npm install typescript -g
2. tsc --version
然后通过ts-node库 ，为ts的运行提供执行环境
1. npm install ts-node -g
2. npm install tslib @type/node -g
3. ts-node 1.ts （运行对应文件
